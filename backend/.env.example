# ========================
# 偏航后端 - 环境变量配置
# 复制本文件为 .env 并填入实际值
# ========================

# 大模型 API（配置后启用真实 AI 推荐，不配置则自动回退模拟模式）
LLM_API_KEY=your_api_key_here
LLM_BASE_URL=https://api.openai-next.com/v1
LLM_MODEL=deepseek-v4-flash

# 跨域允许的来源（逗号分隔，同源部署时可设为 *）
CORS_ORIGINS=*
